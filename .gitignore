# ── Secrets — NEVER commit ─────────────────────────────
# cookie.txt = real NCCN login session cookie
cookie.txt
.env
.dev.vars
*.pem
*.key

# ── Downloaded / generated PDFs (large + © NCCN) ───────
*.pdf
NCCN-*.pdf

# ── Cloudflare / build artifacts ───────────────────────
cf/.wrangler/
cf/node_modules/
node_modules/
# base64 blob, derived from the PNG icons
cf/assets/icons_b64.json

# ── Logs ───────────────────────────────────────────────
cf/*.log
*.log

# ── OS junk ────────────────────────────────────────────
.DS_Store
Thumbs.db

# 核對清單的素材：從 D1 的 page_text dump 出來的衍生檔，隨時可用
# cf/dump_snippet_src.sh 重建。verify 需要它，但不該進版控。
cf/snippets/_src/
