{
  "name": "nccn-download",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vitest": "^3.2.4"
  }
}
